/* Given a string, return true if it ends in "ly". */

public boolean endsLy(String str) {
  return str.endsWith("ly");
  
  //if(str.length() < 2) return false;
  //else return (str.substring(str.length()-2, str.length())).equals("ly");
}
